module.exports = {
	// Use tabs instead of spaces
	useTabs: true,

	// Tab width is only for alignment within tabs
	tabWidth: 4,

	// Semicolons at end of statements
	semi: true,

	// Use single quotes for strings
	singleQuote: false,

	// Add trailing commas where valid in ES5 (objects, arrays, etc.)
	trailingComma: "es5",

	// Print width before wrapping
	printWidth: 120,

	// Keep prettier quiet and consistent
	endOfLine: "lf",
};
